[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "blackroad-hello"

[services.env]
PORT = "8080"
NODE_ENV = "production"
REPO_NAME = "blackroad-hello"
ORG_NAME = "BlackRoad-OS"
